#include <iostream>
#include <vector>
#include "main.h"

const int BLANK{ 0 };
const int X{ 1 };
const int O{ 2 };

std::vector<std::vector<int>> makeBoard(int size = 7) {
    std::vector<std::vector<int>> board(size, std::vector<Cell>) //not done
        return board;
}



bool play(std::vector<std)

int getCurrentToken(const std::vector<std::vector<int>>& board)

int main()
{
    std::cout << "Hello World!\n";
}

